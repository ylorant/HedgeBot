; Plugin definition. Allows it to be loaded
[pluginDefinition]
name = BannedWords

; Plugin main class
mainClass = BannedWords

; Plugin tests class
;testClass = BannedWordsTest

; Plugin default settings
[defaultSettings]
