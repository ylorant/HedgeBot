; Plugin definition. Allows it to be loaded
[pluginDefinition]
name = Counter

; Plugin main class
mainClass = Counter

; Plugin tests class
; testClass = CurrencyTest

; Plugin default settings
[defaultSettings]
