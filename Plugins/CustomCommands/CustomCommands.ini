; Plugin definition. Allows it to be loaded
[pluginDefinition]
name = Custom commands

; Plugin tests class
testClass = CustomCommandsTest

; Plugin classes
mainClass = CustomCommands

; Plugin default settings
[defaultSettings]
