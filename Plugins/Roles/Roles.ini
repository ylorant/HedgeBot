; Plugin definition. Allows it to be loaded
[pluginDefinition]
name = Roles manager

; Plugin classes
mainClass = Roles

; Test class
;testClass = AutoMessageTest

; Plugin default settings
[defaultSettings]
