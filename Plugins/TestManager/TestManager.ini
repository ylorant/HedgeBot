; Plugin definition. Allows it to be loaded
[pluginDefinition]
name = TestManager

; Plugin main class
mainClass = TestManager

; Plugin default settings
[defaultSettings]
testedPlugins =
botName =
logfile =
botConfigDir =
autostart = false
autostop = false
